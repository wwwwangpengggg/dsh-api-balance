# 更新日志

本文件记录每个版本改了什么、为什么。版本号遵循语义化版本：修 bug 走 patch，加功能走 minor
（`0.x` 阶段任何一次破坏性改动都可能出现在 minor 里）。

安装指定版本（把 `#v0.4.1` 换成想要的 tag）：

```powershell
dsh plugin --profile desktop add git+https://github.com/wwwwangpengggg/dsh-api-balance.git#v0.4.1
```

---

## 0.4.1 — 2026-09-22

**升级不再清零。**

- 修：升级到 0.4.0 后第一次重启会显示「今日 0」。旧结构的 `usage.json` 没有"这是哪一天"这个
  字段，被当成新的一天重开，当天早些时候的量被丢掉了。现在会看旧文件最后一次写入是否落在
  今天之内：是就认领那个数字（接上，而不是从零开始），写到更早的日子才丢掉。
  新增 `adoptLegacyUsage` 与两条测试。

## 0.4.0 — 2026-09-22

**token 行从「本次开机」改成「今日累计」。**

- 一天里为了调东西反复开关 DSH 是常态，只报「本次开机」会严重低估，而且和「今日消费」
  不是一个窗口。
- `usage.json` 现在带 `dayKey`，并按「哪一次开机」分条记录（`boots`），跨开机累加成当天合计；
  日界与「今日消费」共用同一个 `dayStartHour`（默认早 8 点起算、24 小时后换新的一天）。
- 顺带修掉一个并发隐患：桌面版与 `dsh web` 同时开着时两边都写同一个 `usage.json`，原来会
  互相盖掉；分条之后各写各的，互不影响。
- 日界在 JS（`usageDayKey`）与 PowerShell（`Get-SpendingDayKey`）各实现一遍，两边测试写死
  同一批边界期望值互为守卫，改一边就会红。

## 0.3.0 — 2026-09-14

**第三页：现在贵不贵。**

- 新增「计费时段」页：高峰 / 优惠（半价），以及还有多久切换。规则取自官方定价页——
  **高峰 = 工作日 01:00–04:00 与 06:00–10:00 UTC，其余时间一律半价**（含午休 12:00–14:00
  与整个周末）。判定一律换算到 UTC 再做，不把本地时刻写死。
  注意这条规则 2026-09-10 变过，旧的「每天 00:30–08:30 错峰优惠」已作废。
- 三页循环：点一下金额区域往后翻一页，到头绕回第 1 页；页码点变成三个；右键菜单改成
  「页面」子菜单，可直接跳页并勾选当前页。
- 新增两个只在排查 / 截图时用的开关：`-NowOverride`（把「现在」钉住）、`-StartPage`（直接
  开在某一页）。
- 修：拖动阈值形同虚设——原来先挪窗口、再判断算不算拖动，于是手抖 1–3px 会把窗口一点点挪走
  （实测连点八次每次偏 3px）。现在越过 3px 才开始跟手。
- 修：窗口自检拿不到单实例互斥量时会静默 `exit 0`，测试只能看到「输出里没有 PASS」；
  现在一律明确报出原因。

## 0.2.2 — 2026-09-12

**跨天不再丢掉小窗关闭期间的花费。**

- 修：换天时把「计数」和「基准线」一起清掉了，于是 DSH 关着那段时间余额掉的部分被永久丢弃
  （现象：官网显示今天花了二十多，卡上只有几毛）。现在换天只把计数归零、**基准线保留**，
  那段减少量记到它第一次被观察到的那一天；只有换币种才整份重开。
- 第 2 页脚注改成「按余额减少量估算 · 充值不计入」，不再让它看起来像账单。
- README 新增「为什么和官网的『今日消费』不一样」。

## 0.2.1 — 2026-09-12

**修一个让第 2 页完全不可用的漏参数。**

- 修：`-SpendPath` 只加进了配置对象、忘了加进 `buildWindowArgs` 的参数数组，宿主从不传它，
  于是窗口拿不到记账文件、第 2 页永远显示「等待取数」，而当时所有测试仍然全绿。现在三个
  状态文件的开关都被逐条断言，删掉任何一个都会立刻变红（已用反证验证过）。
- 「一天」改成早 8 点起算、24 小时后换新的一天（不再是自然日），并把起算时刻显示在页面上。
- 小窗侧加兜底：`-SpendPath` 没传时从 `-StatePath` 同目录自己推出来。
- README 的版本锁定示例改用 `v0.2.1` —— 示例里原来的 `v0.2.0` 正是上面那个坏版本。

## 0.2.0 — 2026-09-12

**第二页：今日消费。** ⚠️ **该版本第 2 页不可用**（原因见 0.2.1），请勿安装。

- 新增「今日消费」页：点一下金额区域在余额 / 今日消费之间切换，右下角两个页码点标出当前页。
- 消费额由余额的减少量倒推（接口只给余额、没有任何账单明细）：充值不计入，跨天或换币种
  从零重开。
- 新增 `-LogicTest`：只跑纯函数、不联网不建窗口，由 Node 侧断言 `LOGICTEST PASS`。

## 0.1.0 — 2026-09-12

**首个可用版本。**

- 桌面角落常驻置顶小窗：DeepSeek API 余额 + 本次开机消耗的 token。
- 七套配色；可以用自己的图片当背景（蒙版浓度五档可调，图片载入即缩到卡片尺寸的 2 倍）。
- 点 `×` 只收进托盘，随时叫回来；位置/主题/背景/蒙版都记在 `state.json`。
- 命名互斥量保证单实例；看门狗盯着父进程 PID，DSH 一退出（含被强杀）小窗跟着消失。
- 高 DPI 原生渲染（125% 下不发虚）、双缓冲 + 变化才重绘（不闪）。
- API Key 只在窗口进程里读，不经过 DSH 的 IPC、不出现在任何命令行里。
